<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ticket.flight.flightapp" >
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".MainActivity" >
            android:label="@string/app_name"
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Airports_Choice_Button" >
            android:label="@string/app_choice">
        </activity>
        <activity android:name=".SearchActivity" >
            android:label="@string/app_search">
        </activity>
        <activity android:name=".HistoryActivity" >
            android:label="@string/app_history">
        </activity>
        <activity android:name=".FavoritesActivity" >
            android:label="@string/app_favs">
        </activity>
        <activity android:name=".PersonalCabinetActivity"
            android:parentActivityName=".MainActivity" >
            android:label="@string/app_personalcab">
        </activity>
        <activity android:name=".SettingsActivity" >
            android:label="@string/app_settings">
        </activity>
        <activity android:name=".AboutActivity" >
            android:label="@string/app_about">
        </activity>
        <activity android:name=".Register" >
            android:label="@string/title_activity_register" >
        </activity>
        <activity android:name=".Login">
            android:label="@string/title_activity_login" >
        </activity>
    </application>

</manifest>
